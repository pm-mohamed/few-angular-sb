import { Meta, Story, Canvas, Description } from '@storybook/addon-docs';
import { componentWrapperDecorator } from '@storybook/angular';
import { PmRadiobuttonComponent } from '../../../projects/innotec/src/lib/components/pm-radiobutton/pm-radiobutton.component.ts';
import { action } from '@storybook/addon-actions';

<Meta title="Components/Radiobutton/Radiobutton-Default" component={PmRadiobuttonComponent} argTypes={{}} parameters={{
  controls: { disabled: true },
}} decorators={[
  //👇 Wraps our stories with a decorator
  componentWrapperDecorator(story => `<div style="margin: 5px;">${story}</div>`),
]}/>

<Description of={PmRadiobuttonComponent} />
<Description markdown={`# Radiobutton`}/>

export const actionsData = {
  onClicked: action('clicked on radio button:'),
};
export const Template = args => ({
  props: {
    ...args,
    onRadiobuttonActivated: actionsData.onClicked
  },
});



This contains the **Radiobuttons**.

## Radiobutton - Default

<Canvas withToolbar isColumn>
  <Story name="Default" args={{
      hovered: false,
      state: false,
      disabled: false
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Radiobutton - Hover

<Canvas withToolbar isColumn>
  <Story name="Hover" args={{
      hovered: true,
      state: false,
      disabled: false
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Radiobutton - Active

<Canvas withToolbar isColumn>
  <Story name="Active" args={{
      hovered: false,
      state: true,
      disabled: false
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Radiobutton - Disabled

<Canvas withToolbar isColumn>
  <Story name="Disabled" args={{
      hovered: false,
      state: false,
      disabled: true
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

<br/><br/>

## Design

[![XD-RadioButtons](https://img.shields.io/badge/-RadioButtons-%235c5c5c?logo=adobexd&labelColor=450034)](https://xd.adobe.com/view/aec6f38b-76a2-4b58-b371-63fac4c7e2fe-fb95/screen/2707f942-aa85-412a-ab85-d777aae10b64/)
